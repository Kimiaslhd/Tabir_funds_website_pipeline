<div dir="rtl">

# پروژه دریافت و ذخیره‌سازی NAV صندوق‌های مبتنی بر تدبیر در SQL Server

## معرفی
این پروژه یک اسکریپت پایتون برای دریافت، پردازش و ذخیره‌سازی اطلاعات تاریخی NAV صندوق‌های سرمایه‌گذاری از **سایت‌های مبتنی بر تدبیر** در **SQL Server** است.

هدف اصلی این کد، خودکارسازی فرآیند جمع‌آوری داده‌های NAV صندوق‌ها از وب‌سایت‌های تدبیر و ذخیره آن‌ها در یک ساختار منظم دیتابیسی برای استفاده در گزارش‌گیری، تحلیل داده، ساخت داشبورد و بررسی روند تاریخی صندوق‌ها است.

این اسکریپت به‌طور کلی:
- لیست صندوق‌های مبتنی بر تدبیر را از دیتابیس می‌خواند
- بر اساس آدرس ثبت‌شده برای هر صندوق، ریشه سایت را استخراج می‌کند
- با توجه به نوع صندوق، Basket ID مناسب را تشخیص می‌دهد یا چند حالت مختلف را بررسی می‌کند
- داده‌های تاریخی NAV را برای یک بازه زمانی شمسی مشخص از صفحات گزارش تدبیر دریافت می‌کند
- جدول یا جدول‌های مربوط به NAV را از ساختار HTML صفحه پیدا می‌کند
- داده‌های استخراج‌شده را تمیز، نرمال و ساختاریافته می‌کند
- از ثبت رکوردهای تکراری جلوگیری می‌کند
- اطلاعات جدید را در SQL Server درج می‌کند
- در صورت وجود رکورد قبلی، اطلاعات را به‌روزرسانی می‌کند

این پروژه می‌تواند به‌عنوان بخشی از یک پایپ‌لاین داده برای صندوق‌های سرمایه‌گذاری استفاده شود و خروجی آن برای داشبوردهای Power BI، تحلیل‌های مالی، گزارش‌های داخلی و پردازش‌های بعدی بسیار کاربردی است.

---

## هدف پروژه
هدف این پروژه، ساخت یک فرآیند نیمه‌خودکار یا خودکار برای دریافت داده‌های تاریخی NAV صندوق‌ها از سایت‌های تدبیر و ذخیره آن‌ها در SQL Server است؛ به شکلی که بتوان این داده‌ها را بعداً برای موارد زیر استفاده کرد:

- گزارش‌گیری دوره‌ای
- تحلیل عملکرد صندوق‌ها
- ساخت داشبوردهای مدیریتی و تحلیلی
- مقایسه روند NAV در بازه‌های مختلف زمانی
- ایجاد دیتاست ساختاریافته برای تحلیل‌های بعدی
- استفاده در پروژه‌های داده‌محور و مالی

---

## تکنولوژی‌ها و ابزارهای استفاده‌شده
در این پروژه از ابزارها و کتابخانه‌های زیر استفاده شده است:

- **Python**
- **SQL Server**
- **pyodbc**
- **requests**
- **jdatetime**
- **urllib3**
- **Regex برای استخراج داده از HTML**
- **داده‌های عمومی وب‌سایت صندوق‌ها**

---

## عملکرد کلی اسکریپت
روند کلی اجرای این پروژه به این صورت است:

1. کاربر تاریخ شروع و تاریخ پایان را به فرمت شمسی وارد می‌کند.
2. اسکریپت به SQL Server متصل می‌شود.
3. لیست صندوق‌هایی که از نوع **Tadbir** هستند از جدول مبنا خوانده می‌شود.
4. برای هر صندوق:
   - اطلاعات پایه صندوق خوانده می‌شود
   - آدرس اصلی سایت از روی `NAV_URL` ساخته یا اصلاح می‌شود
   - بسته به نوع صندوق، Basket ID مناسب انتخاب یا چند گزینه تست می‌شود
   - درخواست‌های لازم به صفحات گزارش NAV ارسال می‌شود
   - جدول مناسب NAV از HTML پیدا می‌شود
   - داده‌ها سطر به سطر استخراج می‌شوند
   - مقادیر خام تمیز و به ساختار استاندارد تبدیل می‌شوند
   - فقط رکوردهای داخل بازه تاریخی موردنظر نگه داشته می‌شوند
5. داده‌های نهایی بررسی می‌شوند تا رکوردهای تکراری حذف شوند
6. اطلاعات در جدول مقصد درج یا به‌روزرسانی می‌شوند
7. در پایان، خلاصه‌ای از تعداد صندوق‌های پردازش‌شده، موفق، ناموفق، تعداد رکوردهای درج‌شده و تعداد رکوردهای به‌روزرسانی‌شده نمایش داده می‌شود

---

## منبع داده
منبع داده این پروژه، **سایت‌های عمومی صندوق‌های سرمایه‌گذاری مبتنی بر تدبیر** است. این سایت‌ها معمولاً صفحات گزارش یا جداولی دارند که اطلاعات NAV صندوق را نمایش می‌دهند.

اسکریپت با استفاده از آدرس ثبت‌شده برای هر صندوق و ساخت URL مناسب، این صفحات را فراخوانی می‌کند و سپس داده‌های موردنیاز را از ساختار HTML آن استخراج می‌کند.

---

## جدول مبنا برای خواندن لیست صندوق‌ها
اسکریپت اطلاعات صندوق‌های تدبیر را از جدول زیر در SQL Server می‌خواند:

```sql
[dbo].[FundsNav_NavUrls]
```

این جدول معمولاً شامل اطلاعات پایه هر صندوق است، از جمله:

- `Dscode`
- `fundName`
- `FundType`
- `NAV_URL`
- `website_Type`

فقط ردیف‌هایی انتخاب می‌شوند که مقدار `website_Type` آن‌ها نشان‌دهنده تدبیر باشد.

این جدول در عمل نقش **مرجع صندوق‌ها و آدرس سایت آن‌ها** را دارد.

---

## جدول مقصد
خروجی نهایی اسکریپت در جدول زیر ذخیره می‌شود:

```sql
[dbo].[FundsNAV_Tadibr]
```

این جدول برای نگهداری داده‌های تاریخی NAV صندوق‌های مبتنی بر تدبیر استفاده می‌شود.

بسته به ساختار نهایی دیتابیس، اطلاعاتی مانند تاریخ، Basket ID، نام صندوق، انواع NAV و سایر شاخص‌های مرتبط در آن ذخیره می‌شوند.

---

## ورودی‌های لازم برای اجرا
برای اجرای صحیح اسکریپت، موارد زیر لازم است:

### 1. نصب کتابخانه‌ها
ابتدا باید کتابخانه‌های موردنیاز نصب شوند:

```bash
pip install requests pyodbc jdatetime urllib3
```

### 2. اطلاعات اتصال به دیتابیس
برای اتصال به SQL Server، این اطلاعات باید در فایل پایتون تنظیم شوند:

- آدرس یا نام سرور
- نام دیتابیس
- نام کاربری
- رمز عبور

### 3. وجود جدول مبنا
جدول `[dbo].[FundsNav_NavUrls]` باید از قبل وجود داشته باشد و اطلاعات صندوق‌ها در آن ثبت شده باشد.

### 4. وجود جدول مقصد
جدول `[dbo].[FundsNAV_Tadibr]` نیز باید از قبل ساخته شده باشد تا داده‌ها در آن ذخیره شوند.

---

## نوع داده‌های استخراج‌شده
این اسکریپت بسته به ساختار سایت صندوق و جدول موجود در صفحه، تلاش می‌کند فیلدهایی مانند موارد زیر را استخراج و ذخیره کند:

- `BasketId`
- `GroupName`
- `FundName`
- `JalaliDate`
- `SubscriptionNAV`
- `CancelNAV`
- `EsmiNAV`
- `NavDiff`
- `TotalSubscriptionUnit`
- `TotalSubscription`
- `TotalCancelUnit`
- `TotalCancel`
- `TotalUnit`
- `StockValueReserve`
- `TotalNetAssetValue`

ممکن است بسته به نوع صندوق و نوع صفحه گزارش، برخی ستون‌ها در بعضی صندوق‌ها وجود داشته باشند و در بعضی دیگر نباشند. به همین دلیل کد طوری نوشته شده که تا حد ممکن ساختارهای مختلف را مدیریت کند.

---

## منطق تشخیص Basket ID
یکی از بخش‌های مهم این پروژه، مدیریت تفاوت ساختار صندوق‌های مختلف مبتنی بر تدبیر است.

از آنجا که بعضی صندوق‌ها داده‌های NAV را در Basket های متفاوتی نمایش می‌دهند، اسکریپت:
- بر اساس نوع صندوق (`FundType`) تصمیم‌گیری می‌کند
- در صورت نیاز، چند Basket ID مختلف را امتحان می‌کند
- تلاش می‌کند از بین پاسخ‌های موجود، مناسب‌ترین داده را پیدا کند

این موضوع باعث می‌شود اسکریپت برای طیف بیشتری از صندوق‌های تدبیر قابل استفاده باشد.

---

## نحوه استخراج اطلاعات از صفحات تدبیر
پس از ساخت URL مناسب و ارسال درخواست به سایت، اسکریپت محتوای HTML برگشتی را بررسی می‌کند.

در این مرحله:
- جدول یا جدول‌های موجود در صفحه شناسایی می‌شوند
- جدول مرتبط با NAV انتخاب می‌شود
- سطرها و ستون‌های جدول خوانده می‌شوند
- داده‌ها از حالت خام به مقادیر قابل ذخیره تبدیل می‌شوند

در بعضی موارد، ساختار HTML سایت‌ها کاملاً یکنواخت نیست. به همین دلیل کد تلاش می‌کند بهترین جدول ممکن را از میان جداول موجود پیدا کند.

---

## پاک‌سازی و نرمال‌سازی داده‌ها
قبل از ذخیره‌سازی نهایی، داده‌های استخراج‌شده پاک‌سازی می‌شوند. این مرحله ممکن است شامل موارد زیر باشد:

- حذف فاصله‌های اضافی
- حذف جداکننده‌های هزارگان
- تبدیل رشته‌ها به عدد
- بررسی مقدارهای خالی
- یکسان‌سازی قالب تاریخ
- کنترل داده‌های ناقص
- حذف رکوردهای تکراری

این مرحله برای بالا بردن کیفیت داده‌ها در جدول مقصد بسیار مهم است.

---

## مدیریت رکوردهای تکراری
از آنجا که ممکن است در فرآیند استخراج، برای یک تاریخ مشخص و یک Basket خاص چند داده مشابه پیدا شود، اسکریپت قبل از ذخیره‌سازی نهایی بررسی می‌کند که:

- رکورد تکراری ثبت نشود
- در صورت وجود چند رکورد مشابه، رکورد کامل‌تر یا معتبرتر نگه داشته شود

این موضوع باعث می‌شود داده‌های ثبت‌شده در دیتابیس تمیزتر و قابل اتکاتر باشند.

---

## درج و به‌روزرسانی اطلاعات در SQL Server
پس از آماده‌سازی داده‌ها، اسکریپت آن‌ها را در جدول مقصد ذخیره می‌کند.

منطق ذخیره‌سازی معمولاً به این شکل است:
- اگر رکوردی برای آن تاریخ، صندوق یا Basket از قبل وجود نداشته باشد، `INSERT` انجام می‌شود
- اگر رکورد از قبل وجود داشته باشد و داده جدید کامل‌تر یا مناسب‌تر باشد، `UPDATE` انجام می‌شود

این روش باعث می‌شود جدول نهایی همواره جدیدتر و کامل‌تر شود.

---

## خروجی و نتیجه اجرا
در پایان اجرای اسکریپت، معمولاً خلاصه‌ای از وضعیت پردازش نمایش داده می‌شود، مانند:

- تعداد کل صندوق‌های بررسی‌شده
- تعداد صندوق‌های موفق
- تعداد صندوق‌های ناموفق
- تعداد رکوردهای درج‌شده
- تعداد رکوردهای به‌روزرسانی‌شده

این خلاصه برای کنترل عملکرد اجرا و بررسی کیفیت فرآیند بسیار مفید است.

---

## کاربردهای این پروژه
خروجی این پروژه می‌تواند در بسیاری از سناریوهای کاری و تحلیلی استفاده شود، از جمله:

- ساخت دیتاست تاریخی NAV صندوق‌ها
- ایجاد بانک اطلاعاتی تحلیلی برای صندوق‌های سرمایه‌گذاری
- استفاده در داشبوردهای Power BI
- مقایسه عملکرد صندوق‌ها در طول زمان
- تهیه گزارش‌های داخلی یا مدیریتی
- پشتیبانی از پروژه‌های داده‌محور در حوزه مالی و سرمایه‌گذاری

---

## نمونه سناریوی استفاده
یک نمونه استفاده از این پروژه می‌تواند به این شکل باشد:

- لیست صندوق‌های تدبیر در جدول مبنا ثبت شده است
- کاربر بازه زمانی شمسی موردنظر را وارد می‌کند
- اسکریپت اطلاعات NAV تاریخی صندوق‌ها را دریافت می‌کند
- داده‌ها در SQL Server ذخیره می‌شوند
- از جدول نهایی برای گزارش‌گیری یا ساخت داشبورد Power BI استفاده می‌شود

---

## ساختار تقریبی فایل‌های مخزن
این مخزن می‌تواند شامل فایل‌های زیر باشد:

- فایل پایتون برای دریافت و ذخیره‌سازی داده‌ها
- فایل SQL برای ساخت جدول مقصد
- فایل‌های کمکی یا نمونه برای جدول مبنا
- فایل README برای توضیح پروژه

---

## نحوه اجرا
برای اجرای پروژه، مراحل زیر را انجام دهید:

1. فایل پایتون را باز کنید  
2. اطلاعات اتصال به SQL Server را وارد کنید  
3. مطمئن شوید جدول‌های موردنیاز در دیتابیس وجود دارند  
4. کتابخانه‌های لازم را نصب کنید  
5. اسکریپت را اجرا کنید  
6. تاریخ شروع و پایان را با فرمت شمسی وارد کنید  
7. منتظر بمانید تا پردازش همه صندوق‌ها کامل شود  
8. نتیجه را در جدول مقصد در SQL Server بررسی کنید  

---

## نکات مهم برای انتشار عمومی در GitHub
اگر این پروژه قرار است به‌صورت عمومی در GitHub منتشر شود، باید قبل از انتشار موارد حساس از کد حذف یا جایگزین شوند، از جمله:

- IP یا نام سرور دیتابیس
- نام دیتابیس
- نام کاربری
- رمز عبور
- مسیرهای داخلی
- هرگونه اطلاعات محرمانه سازمانی
- هرگونه داده خصوصی یا غیرقابل انتشار

بهتر است نسخه عمومی مخزن فقط شامل ساختار کلی پروژه، منطق اجرا، فایل‌های SQL عمومی و کدی باشد که اطلاعات حساس آن حذف شده است.

---

## نکات فنی و محدودیت‌ها
با توجه به اینکه داده‌ها از صفحات وب استخراج می‌شوند، ممکن است در برخی شرایط محدودیت‌هایی وجود داشته باشد، مانند:

- تغییر ساختار HTML سایت صندوق
- در دسترس نبودن موقت سایت
- تفاوت ساختار بین صندوق‌های مختلف
- ناقص بودن برخی داده‌ها در سایت منبع
- تفاوت Basket ID بین انواع صندوق‌ها

به همین دلیل ممکن است لازم باشد در آینده بخش‌هایی از منطق استخراج با توجه به تغییرات سایت‌ها به‌روزرسانی شود.

---

## پیشنهاد برای توسعه‌های بعدی
برای بهبود این پروژه در نسخه‌های بعدی می‌توان این موارد را در نظر گرفت:

- استفاده از HTML Parser قوی‌تر به جای Regex
- اضافه کردن سیستم لاگ‌گیری در فایل
- جداسازی تنظیمات دیتابیس از کد اصلی
- زمان‌بندی اجرای خودکار اسکریپت
- ثبت خطاها و صندوق‌های ناموفق در فایل جداگانه
- بهبود مدیریت خطا و Retry
- تبدیل پروژه به ساختار ماژولارتر
- افزودن تست برای بخش‌های اصلی پردازش

---

## جمع‌بندی
این پروژه یک ابزار کاربردی برای دریافت و ذخیره‌سازی داده‌های تاریخی NAV صندوق‌های مبتنی بر تدبیر است. با استفاده از این اسکریپت می‌توان فرآیند جمع‌آوری داده از سایت‌های صندوق را ساده‌تر، سریع‌تر و ساختاریافته‌تر کرد و داده‌های حاصل را برای تحلیل، گزارش‌گیری و ساخت داشبوردهای مالی به کار برد.

این مخزن به‌صورت عمومی بیشتر برای نمایش ساختار فنی پروژه، نحوه پیاده‌سازی و توانمندی‌های مرتبط با **Python، SQL Server، استخراج داده از وب، پردازش داده و ساخت زیرساخت گزارش‌گیری** منتشر می‌شود.

</div>
